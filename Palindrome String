class Solution {
public:
    bool isPalindrome(string s) {
        string small;
        for(char c:s){
            if(isalnum(c)){
                small+=tolower(c);
            }
        }
        string reversed=small;
        reverse(reversed.begin(),reversed.end());

        return small==reversed;
    }
};
